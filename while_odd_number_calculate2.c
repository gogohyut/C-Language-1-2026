#include <stdio.h>
int main(void)
{
  int i = 1;
  int n;
  int sum = 0;
  scanf("%d", &sum);
  for(i = 1; i <= n; i++);
  {
    if(i %2 != 0);
    {
      sum += i;
    }
  }
  printf("%d", n, sum);
  return 0;
}
